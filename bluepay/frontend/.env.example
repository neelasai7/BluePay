# For local development you can leave this file absent — api.js falls back
# to http://localhost:5000/api automatically.
#
# For production (Vercel/Netlify), set this as an environment variable in
# the hosting dashboard, pointing at your deployed backend:
VITE_API_URL=https://your-backend-url.onrender.com/api
